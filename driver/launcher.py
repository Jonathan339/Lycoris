import download
import browserIntalled


class Louncher:

	def __init__(self, driver:str = 'chrome') ->None:
		self.driver = driver
		self.win_browser_ = RegWindows().browser_installed()

	def __new__(cls):
		pass
		
	def _select_driver(self, any):
		if any( self.)

